export interface UserDepartment{
    clientRole: boolean;
    composite: boolean;
    containerId: string,
    description: string,
    id: string,
    name: string,
    scopeParamRequired: boolean; 
 }